## Extra Slides

---

### Installing and Using gdbgui

```bash
$ python3 -m pip install gdbgui
$ gcc -g -o myprog myprog.c
$ python3 -m gdbgui ./myprog
```

The first step is not necessary on the lab PCs,
since gdbgui is already installed.
